
trainer:
  num_nodes: 2
  strategy: ddp_find_unused_parameters_true
  accelerator: auto
  max_epochs: 500
  enable_checkpointing: true
  check_val_every_n_epoch: 10
  log_every_n_steps: 1
  logger:
    save_dir: ./logs/miniexp
    name: default
    version: null

checkpoint: null
train: true
test: true

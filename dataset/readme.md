# Dataset Layout

This repository intentionally keeps only the dataset skeleton.

Put the real data under the folders below, but do not version large dataset files in this copy:

- `abdata`: clean air-/bone-conducted speech data
- `noisedata`: raw noise sources

Expected layout:

```text
dataset/
  abdata/
  noisedata/
```


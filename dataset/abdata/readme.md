# AB Data

Put the air-bone conducted speech dataset here.

Main path used by the project:

- `dataset/abdata/ABCS_database`

Inside `ABCS_database`:

- `Audio`: clean waveform files split into `train`, `dev`, and `test`
- `script`: transcription and metadata text files split into `train`, `dev`, and `test`

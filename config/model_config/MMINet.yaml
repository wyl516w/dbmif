model:
  name: MMINet

metrics:
  train_metrics:
    mse:
      name: MeanSquaredError
  val_metrics:
    mse:
      name: MeanSquaredError
  test_metrics:
    mse:
      name: MeanSquaredError

trainer:
  check_val_every_n_epoch: 1
  log_every_n_steps: 1